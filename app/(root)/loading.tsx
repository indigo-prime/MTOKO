import Loader from "@/components/Loader";

export default function Home() {
    return (
        <main className="min-h-screen bg-[#333] flex items-center justify-center">
            <Loader />
        </main>
    );
}
